# sweep.yaml
program: train.py
name: sweep_MFCN  # 👈 Nom explicite ici
project: VRP

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}

# On cherche à maximiser la métrique val_accuracy (à adapter)
metric:
  name: test_seg_loss
  goal: minimize

method: bayes   # ou random, bayes...

parameters:
  model:
    values: ["MFCN"]

  model.params.epochs:
    values: [20, 30, 40]

  batch_size:
    values: [16, 32, 64]
  
  data:
    values: ["config7_new"]

  model.params.learning_rate:
    values: [0.001, 0.0005, 0.0001]
  model.freeze:
    values: [true, false]
  model.params.lambda_seg:
    values: [100,1000, 10000]


