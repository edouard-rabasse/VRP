# 1) Valeurs de base
cfg_number: 7
mask: mask_removed
path: data

# 2) Tes chemins, **interpolés** depuis les valeurs ci-dessus
train_original_path: "${path}/train/configuration1_2/"
train_modified_path: "${path}/train/configuration${cfg_number}_2/"

test_original_path:  "${path}/test/configuration1_2/"
test_modified_path:  "${path}/test/configuration${cfg_number}_2/"

train_mask_path:     "${path}/${mask}/train/mask${cfg_number}_2/"
test_mask_path:      "${path}/${mask}/test/mask${cfg_number}_2/"

selection: ## index selection
  type: "range"
  value: [1001,3000]