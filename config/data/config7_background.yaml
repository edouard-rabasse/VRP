# 1) Valeurs de base
name: config7_background
cfg_number: 7
mask: mask_removed
path: data
loader: mask  

# 2) Tes chemins, **interpolés** depuis les valeurs ci-dessus
train_original_path: "${path}/train/configuration1_background/"
train_modified_path: "${path}/train/configuration${cfg_number}_background/"

test_original_path:  "${path}/test/configuration1_background/"
test_modified_path:  "${path}/test/configuration${cfg_number}_background/"

train_mask_path:     "${path}/${mask}/train/mask${cfg_number}_2/"
test_mask_path:      "${path}/${mask}/test/mask${cfg_number}_2/"

selection: ## index selection
  type: "range"
  value: [1001,3000]

augment: false
